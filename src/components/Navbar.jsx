const Navbar = () => {
  return (
    <nav className="max-w-screen-xl px-5 mx-auto">
      <div className="flex items-center justify-center py-6">
        {/* App Name with Logo */}
        <a href="#" className="flex items-center gap-3 text-3xl font-semibold text-[#fafafa] font-inter">
          RenderFlow
        </a>
      </div>
    </nav>
  );
};

export default Navbar;
